# Claude Code adapter

Keep shared Claude settings in `.claude/settings.json`, private overrides in
`.claude/settings.local.json`, and reviewed team MCP definitions in
`.mcp.json`. Inspect project hooks before allowing them to execute.
